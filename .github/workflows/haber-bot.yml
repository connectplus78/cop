name: Haberleri Guncelle

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  guncelle:
    runs-on: ubuntu-latest
    steps:
      - name: Repoyu Klonla
        uses: actions/checkout@v3

      - name: Python Kur
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Gerekli Kütüphaneleri Kur
        run: pip install beautifulsoup4

      - name: Polis Klasörünü Oluştur
        run: mkdir -p polis

      - name: Botu Çalıştır ve JSON Üret
        run: python polis/bot.py

      - name: Güncel Haberleri GitHub'a Gönder
        run: |
          git config --global user.name "Spor Botu"
          git config --global user.email "bot@habersistemi.com"
          git add polis/ntvspor_haberler.json
          git commit -m "Otomatik güncelleme: NTV Spor haberleri çekildi" || echo "Yeni haber yok"
          git push
